<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sylvia.sylviayi_comp304_lab2">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SylviaYi_COMP304_Lab2"
        tools:targetApi="31">
        <activity android:name=".CardInfoActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".TownhouseActivity" />
        <activity android:name=".CondoActivity" />
        <activity android:name=".SemiDetachedHomeActivity" />
        <activity android:name=".DetachedHomeActivity" />
        <activity android:name=".ApartmentActivity" />
        <activity
            android:name=".PropertyTypeSelectionActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>